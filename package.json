{
	"name": "kaboom",
	"scripts": {
		"build": "esbuild --bundle src/main.js --outfile=docs/game.js",
		"dev": "esbuild --bundle src/main.js --outfile=docs/game.js --servedir=docs/ --sourcemap",
		"serve": "esbuild --servedir=."
	},
	"dependencies": {
		"kaboom": "^3000.1.17",
		"tja": "^0.1.3"
	},
	"devDependencies": {
		"esbuild": "^0.19.8"
	}
}
